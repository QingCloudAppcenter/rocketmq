---
- name: copy conf files
  copy:
    src: files/opt/app/
    dest: /opt/app/
    owner: "rocketmq"
    group: "svc"
    mode: "755"
    directory_mode: "750"

- name: install confd files
  include_role:
    name: confd-files